name: learn-github-actions
on: [push]
# on:
#   push:
#     tags:
#       - v1
#       - v1.*
jobs:
  check-bats-version:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v2
        with:
          ref: dev
      - name: Use Node.js
        uses: actions/setup-node@v2
        with:
          node-version: "12.x"
      # - name: Yarn build
      #   run: |
      #     npm install -g yarn
      #   shell: cmd
      # - name: Yarn version
      #   run: |
      #     yarn -v
      #   shell: cmd
      # リリース