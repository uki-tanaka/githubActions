name: learn-github-actions
on: [push]
# on:
#   push:
#     tags:
#       - v1 # Push events to v1 tag
#       - v1.* # Push events to v1.0, v1.1, and v1.9 tags
jobs:
  check-bats-version:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v2
      - name: Use Node.js
        uses: actions/setup-node@v2
        with:
          node-version: "12.x"
      - name: Yarn build
        run: |
          npm install -g yarn
        shell: cmd
      - name: Yarn version
        run: |
          yarn -v
        shell: cmd
      - name: Set Proxy
        run: |
          SET GLOBAL_AGENT_HTTPS_PROXY=%HTTP_PROXY%
        shell: cmd
      - name: Check Proxy
        run: |
          echo %GLOBAL_AGENT_HTTPS_PROXY%
        shell: cmd
      # リリース
      - name: Create release
        id: create_release
        uses: actions/create-release@v1.0.0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: ${{ github.ref }}
          release_name: Release ${{ github.ref }}
          draft: false
          prerelease: false
      # リリース内容の登録その1
      - name: Upload Release Asset1
        id: upload-release-asset
        # 以下の奴は複数回使えないので注意
        uses: actions/upload-release-asset@v1.0.1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ steps.create_release.outputs.upload_url }}
          asset_path: ./test1.vsix
          asset_name: pack.vsix
          asset_content_type: application/vsix
